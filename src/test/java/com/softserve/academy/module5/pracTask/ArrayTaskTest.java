package com.softserve.academy.module5.pracTask;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

//Need to remove randomness for this test(like in GuessNumber) or create methods for every operation with array and test them separately
public class ArrayTaskTest {

    @Test
    public void test() {
        assertTrue(true);
    }

}
